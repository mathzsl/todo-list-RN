import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    borderTopWidth: 0.5,
    borderColor: "#808080",

    alignItems: "center",
    gap: 16,
  },

});
